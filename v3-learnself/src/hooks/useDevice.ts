import { computed } from "vue";
import { useAppStore } from "../store/modules/app";
import { DeviceEnum } from "../constants/app-key";

const useAppStore = 
const isMobile = computed(() =>  === DeviceEnum.Mobile)